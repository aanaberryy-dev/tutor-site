<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Профиль</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f5f6fa; margin: 0; }
    .header { background: #667eea; color: white; padding: 15px 25px; display: flex; justify-content: space-between; }
    .header a { color: white; text-decoration: none; font-size: 14px; margin-left: 15px; }
    .content { padding: 25px; max-width: 500px; margin: 0 auto; }
    .card { background: white; padding: 30px; border-radius: 12px; text-align: center; }
    .avatar { width: 100px; height: 100px; border-radius: 50%; background: #667eea; color: white; font-size: 40px; display: flex; align-items: center; justify-content: center; margin: 0 auto 15px; overflow: hidden; }
    .avatar img { width: 100px; height: 100px; object-fit: cover; }
    .btn { padding: 10px 20px; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold; background: #667eea; color: white; margin-top: 10px; }
    .info { text-align: left; margin-top: 20px; }
    .info p { padding: 8px 0; border-bottom: 1px solid #eee; color: #555; }
    .success { background: #e8f5e9; color: #27ae60; padding: 10px; border-radius: 6px; margin-bottom: 15px; }
  </style>
</head>
<body>
  <div class="header">
    <strong>📚 Кабинет ученика</strong>
    <span>👋 <%= user.name %> <a href="/student/dashboard">📝 Задания</a> <a href="/logout">Выйти</a></span>
  </div>
  <div class="content">
    <h2>👤 Мой профиль</h2>
    <% if (success) { %><div class="success"><%= success %></div><% } %>
    <div class="card">
      <div class="avatar">
        <% if (user.avatar && user.avatar !== 'default.png') { %><img src="/uploads/<%= user.avatar %>"><% } else { %><%= user.name.charAt(0).toUpperCase() %><% } %>
      </div>
      <form action="/student/update-avatar" method="POST" enctype="multipart/form-data">
        <input type="file" name="avatar" accept="image/*" required><br>
        <button type="submit" class="btn">📷 Загрузить фото</button>
      </form>
      <div class="info">
        <p><strong>Имя:</strong> <%= user.name %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        <p><strong>Роль:</strong> 👨‍🎓 Ученик</p>
      </div>
    </div>
  </div>
</body>
</html>