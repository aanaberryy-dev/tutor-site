<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>На проверку</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f5f6fa; margin: 0; }
    .header { background: #667eea; color: white; padding: 15px 25px; display: flex; justify-content: space-between; align-items: center; }
    .header a { color: white; text-decoration: none; font-size: 14px; margin-left: 15px; }
    .menu { background: white; padding: 10px 25px; display: flex; gap: 15px; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }
    .menu a { text-decoration: none; color: #555; font-size: 14px; font-weight: 500; }
    .menu a:hover { color: #667eea; }
    .content { padding: 25px; max-width: 800px; margin: 0 auto; }
    .card { background: white; padding: 20px; border-radius: 12px; margin-bottom: 15px; box-shadow: 0 2px 8px rgba(0,0,0,0.05); display: flex; justify-content: space-between; align-items: center; }
    .card h3 { margin: 0 0 5px 0; }
    .card p { margin: 0; color: #999; font-size: 13px; }
    .btn { padding: 8px 18px; border: none; border-radius: 6px; cursor: pointer; font-size: 13px; font-weight: bold; background: #667eea; color: white; text-decoration: none; }
    .empty { text-align: center; padding: 60px; color: #999; background: white; border-radius: 12px; }
  </style>
</head>
<body>
  <div class="header">
    <strong>📚 Кабинет репетитора</strong>
    <span>👋 <%= user.name %> <a href="/logout">Выйти</a></span>
  </div>
  <div class="menu">
    <a href="/teacher/dashboard">🏠 Главная</a>
    <a href="/teacher/calendar">📅 Расписание</a>
    <a href="/teacher/students">👩‍🎓 Ученики</a>
    <a href="/teacher/check-homeworks" style="color:#667eea; font-weight:600;">📝 На проверку</a>
    <a href="/teacher/finances">💰 Финансы</a>
  </div>
  <div class="content">
    <h2>📝 Домашки на проверку</h2>
    <% if (homeworks.length === 0) { %>
      <div class="empty">✅ Всё проверено! Нет заданий на проверку.</div>
    <% } else { %>
      <% homeworks.forEach(function(hw) { %>
        <div class="card">
          <div>
            <h3><%= hw.title %></h3>
            <p>Ученик: <%= hw.student_name %> | Срок: <%= hw.deadline || '—' %></p>
            <% if (hw.file) { %>
              <p>📎 <a href="/uploads/<%= hw.file %>" target="_blank">Файл</a></p>
            <% } %>
          </div>
          <a href="/teacher/review-homework/<%= hw.id %>" class="btn">📋 Проверить</a>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>